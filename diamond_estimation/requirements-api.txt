# Separate from requirements.txt: the CLI's roi_tool.py needs the
# GUI-enabled opencv-python (cv2.imshow / cv2.namedWindow) to draw
# selection boxes locally. The deployed API never opens a window, and
# GUI opencv often fails to import on minimal Linux server images
# (missing libGL.so.1) — so the API uses opencv-python-headless instead.
opencv-python-headless>=4.8.0
numpy>=1.24.0
scipy>=1.10.0
scikit-image>=0.21.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
